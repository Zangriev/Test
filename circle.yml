
machine:
  python:
    version: 2.7.12
    
test:
  post:
    - python project.py
    - cat /tmp/monfichier.txt | wc -l
    
deployment:
  automerge:
    branch: [dev]
    commands:
      - git checkout origin
      - git pull
      - git merge origin dev
      - git push origin origin