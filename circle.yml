
machine:
  python:
    version: 2.7.12
    
test:
  post:
    - python project.py
    - cat /tmp/monfichier.txt | wc -l
    
deployment:
  branch: origin
    commands:
      - git checkout origin
      - git merge dev
      - git push